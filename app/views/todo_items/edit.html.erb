<%= form_for [@todo_list, @todo_item] do |form| %>
	<% if @todo_item.errors.any? %>
		<h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited the todo item from being saved:</h2>

		<ul>
			<% @todo_item.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	<% end %>

	<%= form.label :content %>
	<%= form.text_field :content %>

	<%= form.submit "Save" %>
<% end %>